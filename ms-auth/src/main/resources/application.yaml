spring:
  application:
    name: KanzoPizzaMsAuth
  profiles:
    active: ${ACTIVE_PROFILE:dev}
    include:
      - common
      - db
      - security
      - client
  main:
    allow-bean-definition-overriding: true
